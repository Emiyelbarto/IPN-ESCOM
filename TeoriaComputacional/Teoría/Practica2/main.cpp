#include <stdio.h>
#include <stdlib.h>
#include "functions.h"
int main(int argc, char const *argv[])
{
	char *Lenguaje, *Aux;
	int Tam;
	Aux = (char *) malloc(sizeof(char) * 1024);
	EscogerLenguaje();
	return 0;
}