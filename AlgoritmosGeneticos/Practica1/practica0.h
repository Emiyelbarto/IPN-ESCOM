#include <stdio.h>
int ConseguirLongitudLista(char * L);           //Función para conseguir la longitud y crear la lista
char * PoblarListaPractica0(int l, char * L);            //Función para poblar aleatoriamente la lista
void MostrarListaPractica0(char * L);                    //Función para mostrar la Lista
char * AcomodarLista(char * L);                 //Función para acomodar la Lista
void ListaBinario(char * L);                    //Función para imprimir la lista en binario
void NumBinario(int n);                         //Función para imprimir un numero entero en binario (16 bits)
void Practica0();
double * CrearLista();
double * PoblarLista(int n, double * L);
double * FuncionCuadrado(double * L);
int LongitudLista(double * L);
void MostrarLista(double * L);
void MostrarHistograma(double *L);
int NumeroMaximo(int l, double *L);